Algoritmo "tabuada"

Var

   n, i, produto: inteiro

Inicio

      produto <- 0
      escreva("Deseja a tabuada para qual valor? ")
      leia(n)
      
      para i de 1 ate 10 faca
           produto <- n * i
           escreval(n," x", i, " =", produto)
      fimpara

Fimalgoritmo